import React from 'react';
import {View, StyleSheet} from 'react-native';

export const PlayController = (props) => (
	<View>

	</View>
);

const styles = StyleSheet.create({

});

export default PlayController;
